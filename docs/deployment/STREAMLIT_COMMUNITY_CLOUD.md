# Streamlit Community Cloud Deployment

Deploy from the public GitHub repository with:

- Repository: `ADiamond12/ai-nutritionist`
- Branch: `main`
- Main file path: `app.py`
- Python dependencies: `requirements.txt`

No secrets are required for the default app. Do not configure persistent feedback storage on a public deployment unless a privacy policy and retention process exist.
